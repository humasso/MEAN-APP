(function () {

    'use strict';

    var ServerNode = require('./index'),
        server = new ServerNode({
            httpPort: 80,
            wsPort: 5000
        });

    server.start('http');
    server.start('ws');


}());
